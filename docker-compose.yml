services:
  frontend:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
    working_dir: /app
    command: npm run dev

# services:
#   frontend:
#     build: .
#     ports:
#       - "5173:80"
#     volumes:
#       - .:/app
#       - /app/node_modules
#     working_dir: /app
#     command: npm run dev -- --host 0.0.0.0